<project name="VFB" default="default" basedir=".">
    <description>
        simple example build file
    </description>
  <!-- set global properties for this build -->
  <property name="src" location="src"/>
  <property name="build" location="build"/>
  <property name="src_dir"  location="WebContent"/>
  <property name="class_dir"  location="${build}/classes"/>
  <property name="web_class_dir"  location="${src_dir}/WEB-INF/classes"/>
  <property name="resources_dir"  location="resources"/>	
  <property name="dest_dir"  location="/usr/local/tomcat-6/webapps/vfb"/>
  <property name="web_resources_dir"  location="${src_dir}/WEB-INF/resources"/>	
  <property name="java.library.path" value="${src_dir}/WEB-INF/lib"/>


  <target name="init">
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${build}"/>
  </target>

  <target name="compile" depends="init"
        description="compile the source " >
  	<echo>(Type 'ant -projecthelp' for more info)</echo>
    <!-- Compile the java code from ${src} into ${build} -->
    <javac srcdir="${src}" destdir="${build}"/>
  </target>

  <target name="copy" depends="init" >
  	<copy todir="${web_class_dir}">
    	<fileset dir="${class_dir}"/>
  	</copy>
  	<copy todir="${web_resources_dir}">
  		<fileset dir="${resources_dir}"/>
  	</copy> 	
  	<copy todir="${dest_dir}">
    	<fileset dir="${src_dir}"/>
  	</copy>
  </target>

  <target name="default" depends="init, copy" />	
	
  <target name="clean"
        description="clean up" >
    <delete dir="${build}"/>
  </target>
  
</project>
